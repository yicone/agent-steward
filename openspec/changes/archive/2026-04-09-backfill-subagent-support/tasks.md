## 1. Backfill review

- [x] 1.1 Review the current Antigravity and Codex subagent implementations for behavior that must remain stable.
- [x] 1.2 Cross-check the viewer summary, search, filter, and rendering behavior that depends on normalized subagent events.

## 2. Artifact alignment

- [x] 2.1 Confirm the proposal accurately scopes the documented capability as `subagent-events`.
- [x] 2.2 Confirm the spec scenarios match the current code and test coverage.
- [x] 2.3 Confirm the design notes capture the current normalization and viewer decisions without inventing new behavior.

## 3. Validation

- [x] 3.1 Run `openspec validate subagent-events --strict` — passed: "Specification 'subagent-events' is valid".
- [x] 3.2 Decide whether any existing project docs should link to the new spec as a normative reference.
